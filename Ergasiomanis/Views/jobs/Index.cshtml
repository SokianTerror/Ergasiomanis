@model IEnumerable<Ergasiomanis.Models.jobs>

<div>
@{
    ViewBag.Title = "Index";
}
    </hr>
    <hr/>
</div>
<h2>Search</h2>
<form>
   <table class="table">
       <tr><td>Job Id</td><td>Job Desctiption</td><td>From Min Level</td><td>To Min Level</td><td>From Max Level</td><td>To Max Level</td></tr>
       <tr><td><input type="number" id="jobId" name="jobId" /></td><td><input type="text" id="jobDesc" name="jobDesc"/></td><td><input type="number" id="FromMinlvl" name="FromMinlvl" /></td>
       <td><input type="number" id="ToMinlvl" name="ToMinlvl"/></td><td><input type="number" id="FromMaxlvl" name="FromMaxlvl"/></td><td><input type="number" id="ToMaxlvl" name="ToMaxlvl"/></td></tr>
       <tr><td><button class="buttona" type="submit">Search</button>   <button class="buttona" type="reset">Reset</button></td></tr>
   </table>
    
</form>
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.job_desc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.min_lvl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.max_lvl)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.job_desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.min_lvl)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.max_lvl)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.job_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.job_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.job_id })
        </td>
    </tr>
}

</table>
